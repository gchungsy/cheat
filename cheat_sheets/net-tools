# net-tools
# A collection of programs for controlling network settings in Linux.

# Display all network interfaces and their statuses:
ifconfig

# Display routing table:
route -n

# Display active TCP/UDP network connections:
netstat -tuln

# Show ARP cache:
arp -a

# Display all listening ports:
netstat -l
